variables:
  SEMGREP_LANG: 'python'

include:
  - local: '/global/stages/20-security/docker.yaml'
  - local: '/python/abstracts/pdm.yaml'

sca:safety:
  extends: '.pdm'
  stage: 'security (sca/sast)'
  script:
    - pdm run safety-check
