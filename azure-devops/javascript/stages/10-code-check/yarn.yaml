parameters:
  - name: 'INSTALL_RUN_SCRIPTS'
    type: 'boolean'
    default: false
  - name: 'RUN_BEFORE_CODE_CHECK'
    type: 'string'
    default: 'echo "No before script"'
  - name: 'RUN_AFTER_CODE_CHECK'
    type: 'string'
    default: 'echo "No after script"'
  - name: 'WORKING_DIRECTORY'
    type: 'string'
    default: ''
  - name: 'DEPENDS_ON'
    type: object
    default: ''

stages:
  - stage: 'code_check'
    displayName: 'code check (style/quality)'
    condition: not(startsWith(variables['Build.SourceBranch'], 'refs/tags/'))
    ${{ if ne(parameters.DEPENDS_ON, '') }}:
      dependsOn: ${{ parameters.DEPENDS_ON }}
    jobs:
      - template: '../../../global/stages/10-code-check/basic-checks.yaml'

      - job: 'style_eslint'
        displayName: 'style:eslint'
        steps:
          - template: 'steps/eslint.yaml'
            parameters:
              WORKING_DIRECTORY: "${{ parameters.WORKING_DIRECTORY }}"
              RUN_BEFORE_CODE_CHECK: ${{ parameters.RUN_BEFORE_CODE_CHECK }}
              RUN_AFTER_CODE_CHECK: ${{ parameters.RUN_AFTER_CODE_CHECK }}

      - job: 'quality_knip'
        displayName: 'quality:knip'
        steps:
          - template: '../../abstracts/corepack-node20.18.3-no-cache.yaml'

          # The hook's VALUE is shell, supplied by the consumer's pipeline. Passing it
          # through `env:` and running it with `eval` keeps it DATA at template-expansion
          # time: Azure substitutes `${{ }}` into the YAML before the agent ever sees it,
          # so a value containing a newline or a quote used to be able to terminate this
          # step and append steps of its own. Through `env:` the shell only expands it at
          # run time, which is the same fix already applied to `pipelines_ref` in
          # .github/workflows/terra.yaml.
          - bash: 'eval "$PIPELINES_HOOK"'
            displayName: 'Before code check'
            workingDirectory: "${{ parameters.WORKING_DIRECTORY }}"
            env:
              PIPELINES_HOOK: ${{ parameters.RUN_BEFORE_CODE_CHECK }}

          # Installed WITHOUT lifecycle scripts. `yarn install` (Yarn 2+ runs them by
          # default) and `npm ci` otherwise execute `postinstall` from every package in
          # the transitive tree, as the CI user, before any of this project's own code
          # runs. Projects that genuinely need them pass `INSTALL_RUN_SCRIPTS: true` and
          # get `rebuild` afterwards, once the tree is on disk.
          #
          # The no-lockfile branch now FAILS instead of falling back to `npm install`.
          # A resolution performed at CI time is not the resolution anyone reviewed, so
          # the job was silently testing a dependency set that exists nowhere else --
          # and `--ignore-scripts` cannot make an unpinned tree safe.
          - script: |
              set -e
              if [ -f yarn.lock ]; then
                yarn install --immutable --check-cache --mode=skip-build
                if [ "$INSTALL_RUN_SCRIPTS" = 'True' ] || [ "$INSTALL_RUN_SCRIPTS" = 'true' ]; then
                  yarn rebuild
                fi
              elif [ -f package-lock.json ]; then
                npm ci --ignore-scripts
                if [ "$INSTALL_RUN_SCRIPTS" = 'True' ] || [ "$INSTALL_RUN_SCRIPTS" = 'true' ]; then
                  npm rebuild
                  npm run prepare --if-present
                fi
              else
                echo "ERROR: no yarn.lock and no package-lock.json found."
                echo "Commit a lockfile so CI installs the dependency set you reviewed."
                exit 1
              fi
            displayName: 'Install dependencies'
            workingDirectory: "${{ parameters.WORKING_DIRECTORY }}"
            # Azure stringifies a `boolean` parameter as `True`/`False`, which is why
            # both spellings are accepted above.
            env:
              INSTALL_RUN_SCRIPTS: ${{ parameters.INSTALL_RUN_SCRIPTS }}

          # Pinned; `npx --yes knip` executes the newest published knip on
          # every run. See global/scripts/shared/pinned-versions.sh.
          - script: 'npx --yes --ignore-scripts knip@6.32.2'
            displayName: 'Run Knip unused code detection'
            workingDirectory: "${{ parameters.WORKING_DIRECTORY }}"

          # The hook's VALUE is shell, supplied by the consumer's pipeline. Passing it
          # through `env:` and running it with `eval` keeps it DATA at template-expansion
          # time: Azure substitutes `${{ }}` into the YAML before the agent ever sees it,
          # so a value containing a newline or a quote used to be able to terminate this
          # step and append steps of its own. Through `env:` the shell only expands it at
          # run time, which is the same fix already applied to `pipelines_ref` in
          # .github/workflows/terra.yaml.
          - bash: 'eval "$PIPELINES_HOOK"'
            displayName: 'After code check'
            workingDirectory: "${{ parameters.WORKING_DIRECTORY }}"
            env:
              PIPELINES_HOOK: ${{ parameters.RUN_AFTER_CODE_CHECK }}
        continueOnError: true
