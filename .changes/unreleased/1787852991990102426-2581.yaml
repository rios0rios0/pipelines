kind: 'Added'
body: 'added `claude-review.yaml` and `claude-code.yaml` as reusable workflows, so the Claude automated PR review and the `@claude` mention responder now live beside every other reusable workflow instead of in `rios0rios0/.github`. Consumers call them with `uses: ''rios0rios0/pipelines/.github/workflows/claude-review.yaml@main''` and pass `CLAUDE_CODE_OAUTH_TOKEN` explicitly.'
time: '2026-08-27T17:49:51.989984255Z'
