include:
  - local: '/gitlab/java/abstracts/gradle.yaml'

.gradle-library:
  extends: '.gradle'
  cache:
    paths:
      - '.gradle'
      - '.gradle-home/caches'
      - '.gradle-home/notifications'
      - 'build'
      - 'lib/build'
