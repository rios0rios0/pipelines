kind: 'Changed'
body: 'changed this repository''s own Claude callers to `claude-review.yaml` and `claude-mention.yaml`, the same names every consuming repository uses, so this repository is no longer a special case. They call the local `reusable-` definitions and pass `CLAUDE_CODE_OAUTH_TOKEN` explicitly instead of `secrets: inherit`, which fails Semgrep''s `yaml.github-actions.security.secrets-inherit` rule. The callers are named `Claude Code Review` and `Claude Mention` and single-quote their `types:` entries, per the account YAML standard.'
time: '2026-08-27T17:49:51.989984255Z'
