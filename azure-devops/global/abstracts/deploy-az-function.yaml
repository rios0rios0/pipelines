parameters:
  - name: PIPELINE_FIREWALL_NAME
    type: 'string'
  - name: RESOURCE_GROUP_NAME
    type: 'string'
  - name: ENVIRONMENT
    type: 'string'
  - name: AZM_SERVICE_CONNECTION
    type: 'string'
  - name: AZURE_DEPLOY_OUTPUTS
    type: 'object'
  - name: MIGRATIONS_CACHE
    type: 'string'
  - name: SEEDERS_CACHE
    type: 'string'
  # - name: CONNECTION_STRING
  #   type: 'string'
  #   default: ''
  # - name: DATABASE_SERVER_NAME
  #   type: 'string'
  #   default: ''

steps:
  - template: './database.yaml'
    parameters:
      PIPELINE_FIREWALL_NAME: ${{ parameters.PIPELINE_FIREWALL_NAME }}
      RESOURCE_GROUP_NAME: ${{ parameters.RESOURCE_GROUP_NAME }}
      ENVIRONMENT: ${{ parameters.ENVIRONMENT }}
      AZM_SERVICE_CONNECTION: ${{ parameters.AZM_SERVICE_CONNECTION }}
      OUTPUTS: ${{ parameters.AZURE_DEPLOY_OUTPUTS }}
      MIGRATIONS_CACHE: ${{ parameters.MIGRATIONS_CACHE }}
      SEEDERS_CACHE: ${{ parameters.SEEDERS_CACHE }}
      # CONNECTION_STRING: ${{ parameters.CONNECTION_STRING }}
      # DATABASE_SERVER_NAME: ${{ parameters.DATABASE_SERVER_NAME }}

  - script: |
      set -e
      siteName=$(echo $AZURE_DEPLOY_OUTPUTS | jq -r '.siteName.value')
      echo "##vso[task.setvariable variable=siteName]$siteName"
    displayName: 'Getting Pipeline Variables'
    env:
      AZURE_DEPLOY_OUTPUTS: ${{ parameters.AZURE_DEPLOY_OUTPUTS }}

  # - task: 'AzureCLI@2'
  #   inputs:
  #     azureSubscription: ${{ parameters.AZM_SERVICE_CONNECTION }}
  #     scriptType: 'bash'
  #     scriptLocation: 'inlineScript'
  #     inlineScript: |
  #       set -e
  #       SETTINGS=""
  #       for var in $(compgen -e); do
  #         if [[ $var == REPLACE_* ]]; then
  #           if [[ -z ${!var} ]]; then
  #             echo "Warning: $var isn't defined"
  #           else
  #             setting_name=${var#REPLACE_}
  #             SETTINGS+="$setting_name=${!var} "
  #           fi
  #         fi
  #       done
  #       if [[ -z $SETTINGS ]]; then
  #         echo "Warning: There are no environment variables to update"
  #       else
  #         az functionapp config appsettings set \
  #           --name "$(siteName)" \
  #           --resource-group "${{ parameters.RESOURCE_GROUP_NAME }}" \
  #           --settings $SETTINGS
  #       fi
  #   displayName: 'Replace Azure Function Variables'

  # - task: 'AzureCLI@2'
  #   displayName: 'Publish Function'
  #   inputs:
  #     azureSubscription: ${{ parameters.AZM_SERVICE_CONNECTION }}
  #     scriptType: 'bash'
  #     scriptLocation: 'inlineScript'
  #     inlineScript: |
  #       set -e
  #       az functionapp deployment source config-zip \
  #         --name "$(siteName)" \
  #         --resource-group "${{ parameters.RESOURCE_GROUP_NAME }}" \
  #         --src "$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip"
  #   retryCountOnTaskFailure: 3
